<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_primary"
    tools:context=".MainActivity">

    <!-- ==================== 顶部状态栏 ==================== -->
    <LinearLayout
        android:id="@+id/layout_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="16dp"
        android:paddingTop="12dp"
        android:paddingBottom="8dp"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="麻将助手"
            android:textColor="@color/text_primary"
            android:textSize="20sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_status_badge"
            android:paddingHorizontal="12dp"
            android:paddingVertical="4dp"
            android:text="已连接"
            android:textColor="@color/accent_green"
            android:textSize="13sp" />
    </LinearLayout>

    <!-- ==================== 信息展示区域 (上半部分) ==================== -->
    <LinearLayout
        android:id="@+id/layout_info_area"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:paddingHorizontal="12dp"
        android:paddingTop="4dp"
        app:layout_constraintTop_toBottomOf="@id/layout_header"
        app:layout_constraintBottom_toTopOf="@id/layout_camera_area">

        <!-- 手牌区域 -->
        <LinearLayout
            android:id="@+id/layout_user_hand"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="2"
            android:layout_marginBottom="6dp"
            android:background="@drawable/bg_card_dark"
            android:orientation="vertical"
            android:paddingHorizontal="12dp"
            android:paddingVertical="8dp">

            <TextView
                android:id="@+id/tv_title_hand"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="🀄 手牌"
                android:textColor="@color/text_secondary"
                android:textSize="13sp" />

            <TextView
                android:id="@+id/tv_content_hand"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:gravity="center"
                android:text=""
                android:textColor="@color/text_primary"
                android:textSize="16sp" />
        </LinearLayout>

        <!-- 副露区域 -->
        <LinearLayout
            android:id="@+id/layout_suggested"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1.5"
            android:layout_marginBottom="6dp"
            android:background="@drawable/bg_card_dark"
            android:orientation="vertical"
            android:paddingHorizontal="12dp"
            android:paddingVertical="8dp">

            <TextView
                android:id="@+id/tv_title_suggested"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="📋 副露"
                android:textColor="@color/text_secondary"
                android:textSize="13sp" />

            <TextView
                android:id="@+id/tv_content_suggested"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:gravity="center"
                android:text=""
                android:textColor="@color/text_primary"
                android:textSize="14sp" />
        </LinearLayout>

        <!-- 操作建议区域 -->
        <LinearLayout
            android:id="@+id/layout_waiting"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="3"
            android:background="@drawable/bg_card_dark"
            android:orientation="vertical"
            android:paddingHorizontal="12dp"
            android:paddingVertical="8dp">

            <TextView
                android:id="@+id/tv_title_waiting"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="💡 操作建议"
                android:textColor="@color/accent_gold"
                android:textSize="13sp" />

            <ScrollView
                android:id="@+id/sv_content_waiting"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:layout_marginTop="4dp"
                android:fillViewport="true"
                android:scrollbars="vertical">

                <TextView
                    android:id="@+id/tv_content_waiting"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text=""
                    android:textColor="@color/text_primary"
                    android:textSize="16sp"
                    android:lineSpacingExtra="4dp" />
            </ScrollView>
        </LinearLayout>
    </LinearLayout>

    <!-- ==================== 相机预览区域 (下半部分) ==================== -->
    <FrameLayout
        android:id="@+id/layout_camera_area"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <!-- 相机预览 TextureView -->
        <TextureView
            android:id="@+id/view_camera_preview"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />


        <!-- 拍摄引导矩形框：中间透明，帮助用户确认取景范围 -->
        <View
            android:id="@+id/view_guide_frame"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:layout_marginHorizontal="24dp"
            android:layout_marginVertical="48dp"
            android:background="@drawable/bg_guide_frame" />

        <!-- 拍摄提示文字 -->
        <TextView
            android:id="@+id/tv_instruction_camera_preview"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|center_horizontal"
            android:layout_marginTop="8dp"
            android:background="@drawable/bg_toast_dark"
            android:paddingHorizontal="16dp"
            android:paddingVertical="6dp"
            android:text="将手牌放入框内"
            android:textColor="@color/text_primary"
            android:textSize="12sp" />

        <!-- 返回按钮（左上角） -->
        <TextView
            android:id="@+id/btn_camera_back"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|start"
            android:layout_marginTop="12dp"
            android:layout_marginStart="12dp"
            android:background="@drawable/bg_toast_dark"
            android:paddingHorizontal="16dp"
            android:paddingVertical="8dp"
            android:text="✕ 返回"
            android:textColor="@color/text_primary"
            android:textSize="14sp"
            android:elevation="6dp" />

        <!-- FAB 拍照按钮 -->
        <FrameLayout
            android:id="@+id/btn_shutter"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_gravity="bottom|center_horizontal"
            android:layout_marginBottom="16dp"
            android:elevation="6dp">

            <!-- 外圈 -->
            <View
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/bg_fab_shutter"
                android:alpha="0.3" />
            <!-- 内圈 -->
            <View
                android:layout_width="52dp"
                android:layout_height="52dp"
                android:layout_gravity="center"
                android:background="@drawable/bg_fab_shutter" />
        </FrameLayout>

        <!-- 快门闪光 overlay（拍照时短暂白闪） -->
        <View
            android:id="@+id/view_shutter_flash"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/shutter_flash"
            android:visibility="gone" />
    </FrameLayout>

    <!-- ==================== 照片预览确认区域 (覆盖层) ==================== -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/layout_photo_review_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/overlay_dark"
        android:elevation="15dp"
        android:visibility="gone">

        <ImageView
            android:id="@+id/image_photo_review"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_margin="24dp"
            android:scaleType="fitCenter"
            app:layout_constraintBottom_toTopOf="@+id/layout_review_buttons"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 分割参考线 (50%) -->
        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:background="@color/guide_line"
            app:layout_constraintEnd_toEndOf="@id/image_photo_review"
            app:layout_constraintStart_toStartOf="@id/image_photo_review"
            app:layout_constraintTop_toTopOf="@id/image_photo_review"
            app:layout_constraintBottom_toBottomOf="@id/image_photo_review" />

        <TextView
            android:id="@+id/tv_instruction_review"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:text="确认照片"
            android:textColor="@color/text_secondary"
            android:textSize="14sp"
            app:layout_constraintBottom_toTopOf="@+id/layout_review_buttons"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- 确认/取消按钮组 -->
        <LinearLayout
            android:id="@+id/layout_review_buttons"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="32dp"
            android:layout_marginBottom="32dp"
            android:gravity="center"
            android:orientation="horizontal"
            app:layout_constraintBottom_toBottomOf="parent">

            <Button
                android:id="@+id/btn_review_cancel"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="1"
                android:layout_marginEnd="8dp"
                android:background="@drawable/bg_btn_danger"
                app:backgroundTint="@null"
                android:text="重拍"
                android:textColor="@color/white"
                android:textSize="16sp" />

            <Button
                android:id="@+id/btn_review_send"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:background="@drawable/bg_btn_primary"
                app:backgroundTint="@null"
                android:text="发送分析"
                android:textColor="@color/white"
                android:textSize="16sp" />
        </LinearLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- ==================== 底部控制栏 ==================== -->
    <LinearLayout
        android:id="@+id/layout_controls"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal"
        android:paddingHorizontal="16dp"
        android:paddingVertical="12dp"
        app:layout_constraintBottom_toBottomOf="parent">

        <!-- 主操作按钮 (开始/结束对局) -->
        <Button
            android:id="@+id/btn_action_primary"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:layout_marginEnd="6dp"
            android:background="@drawable/bg_btn_primary"
            app:backgroundTint="@null"
            android:text="开始对局"
            android:textColor="@color/white"
            android:textSize="15sp"
            android:textStyle="bold" />

        <!-- 拍照按钮 -->
        <Button
            android:id="@+id/btn_action_secondary"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:layout_marginHorizontal="6dp"
            android:background="@drawable/bg_btn_secondary"
            app:backgroundTint="@null"
            android:text="📷 拍照分析"
            android:textColor="@color/white"
            android:textSize="15sp"
            android:visibility="gone" />

        <!-- 语音录制按钮 -->
        <Button
            android:id="@+id/btn_action_record"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:layout_marginStart="6dp"
            android:background="@drawable/bg_btn_secondary"
            app:backgroundTint="@null"
            android:text="🎤 按住说话"
            android:textColor="@color/white"
            android:textSize="15sp"
            android:visibility="gone" />
    </LinearLayout>

    <!-- ==================== 加载指示器 ==================== -->
    <FrameLayout
        android:id="@+id/layout_loading"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/overlay_dark"
        android:elevation="20dp"
        android:visibility="gone">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:orientation="vertical">

            <ProgressBar
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:indeterminateTint="@color/accent_green" />

            <TextView
                android:id="@+id/tv_loading_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:text="正在分析手牌..."
                android:textColor="@color/text_primary"
                android:textSize="14sp" />
        </LinearLayout>
    </FrameLayout>

    <!-- ==================== Custom Toast ==================== -->
    <TextView
        android:id="@+id/tv_custom_toast"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="80dp"
        android:background="@drawable/bg_toast_dark"
        android:elevation="25dp"
        android:paddingHorizontal="20dp"
        android:paddingVertical="10dp"
        android:text="Toast Message"
        android:textColor="@color/text_primary"
        android:textSize="14sp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- ==================== 隐藏的滚动按钮 (仅保留向后兼容,不再显示) ==================== -->
    <Button
        android:id="@+id/btn_scroll_up"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/btn_scroll_down"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>